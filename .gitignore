# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.env

# Test / coverage artifacts
.coverage
coverage.xml
htmlcov/
.pytest_cache/

# Build artifacts
dist-architecture/
node_modules/

# Lambda packaging (built locally, not committed)
/tmp/lambda-pkg/
/tmp/lambda.zip

# CloudFormation packaged output (generated by cfn package, not committed)
infrastructure/cloudformation/master-packaged.yml
infrastructure/cloudformation/master-secondary-packaged.yml

# Temporary debug/error output files
stackError.txt
security-scan-report-*.md

# Microsoft Office lock files (created while a file is open, auto-removed on close)
~$*.xlsx
~$*.docx
~$*.pptx

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
*.swo

# Codex - ChatGPT code generation
.codex/

# PENDING REVIEW — restored from private repo, sanitized (account ID -> placeholder),
# but not yet reviewed/approved for public commit. Remove these lines once reviewed.
docs/
diagrams/
